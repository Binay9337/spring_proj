spring.application.name=expense-tracker

spring.datasource.url=jdbc:mysql://localhost:3306/sb_aug
spring.datasource.username=root
spring.datasource.password=root

# it is use  to read schema and data sql file it always. it is  never by default
# spring.sql.init.mode=always

# after work findished
spring.sql.init.mode=never

# when u dont have sql then also things can be done by this
# spring.sql.init.mode=embedded